[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "odos-ermou-recommendations"
version = "0.1.0"
description = "Local market-basket, graph, and embedding recommendations for a toy shop"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "networkx",
    "numpy",
    "openpyxl",
    "pandas",
    "streamlit",
]

[project.scripts]
odos-recommend = "serve_recommendations:main"
odos-app = "streamlit_app:launch"
odos-pipeline = "main:main"
odos-tune = "phase4_experiments:main"
odos-analyze = "phase6_analysis:main"
odos-metadata-transfer = "metadata_transfer_experiment:main"
odos-visualize = "graph_visualizations:main"
odos-improve = "experiment_runner:main"
odos-improvement-plots = "visualization:main"
odos-evaluate-product-page = "single_sku_evaluation:main"
odos-evaluate-future = "future_period_evaluation:main"

[tool.setuptools]
py-modules = ["main", "serve_recommendations", "streamlit_app", "phase4_experiments", "phase6_analysis", "metadata_transfer_experiment", "graph_visualizations", "experiment_runner", "visualization", "single_sku_evaluation", "future_period_evaluation"]

[tool.setuptools.packages.find]
include = ["src", "src.*"]
